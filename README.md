# Система Управления Экспериментами

Полнофункциональная система для управления экспериментами по анализу устойчивости полимерных плёнок к царапинам.

## 🎯 Возможности

### Управление Экспериментами
- ✅ Создание экспериментов с параметрами (тип плёнки, конфигурация оборудования, вес, наличие ткани)
- ✅ Загрузка эталонных изображений с выбором ROI (Region of Interest)
- ✅ Добавление изображений после царапин с указанием количества проходов
- ✅ Просмотр результатов в виде графиков и карусели изображений
- ✅ Экспорт данных

### Анализ Изображений
- ✅ Автоматическое преобразование RGB → Grayscale
- ✅ Построение гистограммы яркости (0-255)
- ✅ Расчёт индекса царапин (Scratch Index) методом линейной свёртки
- ✅ Оценка качества: Отлично (<0.05), Хорошо (0.05-0.15), Плохо (>0.30)
- ✅ Визуализация результатов с помощью графиков Recharts

### Администрирование
- ✅ Управление пользователями (создание, редактирование, деактивация)
- ✅ Управление типами плёнок (название, производитель, описание)
- ✅ Управление конфигурациями оборудования (параметры в JSON)
- ✅ Интуитивная панель администратора с вкладками

### Аутентификация и Безопасность
- ✅ JWT токены (Access + Refresh)
- ✅ Хеширование паролей с bcrypt
- ✅ Разделение баз данных (users_db + experiments_db)
- ✅ CORS защита
- ✅ Валидация данных с Pydantic v2

## 🏗️ Технологический Стек

### Backend
- **FastAPI** - Современный асинхронный веб-фреймворк
- **SQLAlchemy 2.0** - ORM с async поддержкой
- **PostgreSQL** - Две раздельные базы данных
- **Redis** - Кеширование с CachedRepositoryImpl
- **Alembic** - Миграции базы данных
- **Pydantic v2** - Валидация и сериализация
- **Python-JOSE** - JWT токены
- **Passlib + Bcrypt** - Безопасное хеширование паролей
- **NumPy + Pillow** - Обработка изображений

### Frontend
- **React 18** - Функциональные компоненты с Hooks
- **React Router** - Навигация
- **Recharts** - Интерактивные графики
- **Webpack 5** - Сборка (без Vite/CRA)
- **Axios** - HTTP клиент с interceptors

### DevOps
- **Docker + Docker Compose** - Контейнеризация
- **Nginx** - Reverse proxy и статический контент
- **Multi-stage builds** - Оптимизированные образы

## 📐 Архитектура

### Backend (SOLID Principles)

```
src/
├── api/                    # API Endpoints (роутеры)
│   ├── auth.py            # Аутентификация
│   ├── users.py           # Управление пользователями
│   ├── experiments.py     # Эксперименты
│   ├── films.py           # Типы плёнок
│   └── equipment_configs.py
├── core/                   # Ядро приложения
│   ├── config.py          # Настройки (Pydantic Settings)
│   ├── database.py        # Подключения к БД
│   ├── security.py        # JWT, bcrypt
│   ├── dependencies.py    # Dependency Injection
│   └── init_data.py       # Инициализация (admin user)
├── models/                 # SQLAlchemy ORM Models
│   ├── user.py
│   ├── experiment.py
│   ├── film.py
│   └── equipment_config.py
├── repositories/           # Repository Pattern
│   ├── base.py            # BaseRepository
│   ├── cached.py          # CachedRepositoryImpl
│   └── user_repository.py
├── services/               # Business Logic
│   ├── base.py            # BaseService
│   ├── user_service.py    # UserService с аутентификацией
│   ├── experiment_service.py
│   └── image_analysis_service.py  # Алгоритм анализа
└── schemas/                # Pydantic Schemas
    ├── user.py
    ├── experiment.py
    └── responses.py        # Unified Response Format
```

### Frontend (Services Pattern)

```
frontend/src/
├── components/             # Переиспользуемые компоненты
│   ├── Layout/            # Header, Footer, Navigation
│   ├── ROISelector/       # Canvas для выбора ROI
│   ├── ImageCarousel/     # Карусель изображений
│   └── ScratchChart/      # График индекса царапин
├── pages/                  # Страницы приложения
│   ├── LoginPage.jsx      # Вход/Регистрация
│   ├── ExperimentsPage.jsx # Список экспериментов
│   ├── CreateExperimentPage.jsx  # Мультишаговая форма
│   ├── ExperimentDetailPage.jsx  # Детали + анализ
│   └── AdminPage.jsx      # Админ-панель
├── services/api/           # API сервисы (SOLID)
│   ├── HttpClient.js      # Базовый HTTP клиент
│   ├── AuthService.js     # Аутентификация
│   ├── UserService.js     # Пользователи
│   ├── ExperimentService.js
│   ├── FilmService.js
│   └── ConfigService.js
├── context/                # React Context
│   └── AuthContext.js     # Глобальное состояние auth
└── styles/                 # CSS модули
    ├── theme.css          # Design tokens
    └── pages/             # Стили страниц
```

## 🚀 Быстрый Старт

### Предварительные Требования

- Docker Desktop
- Node.js 16+ (для локальной разработки frontend)
- Python 3.11+ (для локальной разработки backend)

### Установка и Запуск

```bash
# 1. Клонировать репозиторий
cd C:\Users\Prival\Desktop\Scratch

# 2. Настроить переменные окружения
# Отредактируйте .env (уже настроен)

# 3. Запустить через Docker
docker compose up -d --build

# 4. Проверить статус
docker compose ps

# 5. Открыть приложение
# Frontend: http://localhost
# API Docs: http://localhost/api/docs
```

### Локальная Разработка

```bash
# Backend
docker compose up -d experiments_db users_db redis
python -m uvicorn src.main:app --reload --host localhost --port 8000

# Frontend (в новом терминале)
cd frontend
npm install
npm start
# http://localhost:3000
```

## 👤 Вход в Систему

**Администратор по умолчанию:**
- Username: `admin`
- Password: `Akrawer1`

⚠️ Измените пароль после первого входа!

## 📊 Алгоритм Анализа

### Расчёт Индекса Царапин

1. **Преобразование в Grayscale**
   ```
   Grayscale(i,j) = 0.3*R + 0.59*G + 0.11*B
   ```

2. **Построение гистограммы** яркости (0-255)

3. **Средняя яркость**
   ```
   h(q)_ср = P_qmax / P
   где P_qmax - количество пикселей с доминирующей яркостью
       P - общее количество пикселей
   ```

4. **Индекс царапин** (линейная свёртка)
   ```
   U(X) = Σ(w_i * x_i)
   где w_i = q/255 (вес увеличивается с яркостью)
   ```

### Интерпретация Результатов

| Индекс | Оценка | Цвет |
|--------|--------|------|
| < 0.05 | Отлично | 🟢 Зелёный |
| 0.05 - 0.15 | Хорошо | 🔵 Синий |
| > 0.30 | Плохо | 🔴 Красный |

## 📁 Структура Проекта

```
Scratch/
├── backend/
│   ├── src/                # Исходный код
│   ├── alembic/            # Миграции БД
│   ├── requirements.txt    # Python зависимости
│   ├── Dockerfile          # Multi-stage build
│   └── entrypoint.sh       # Startup script
├── frontend/
│   ├── src/                # React приложение
│   ├── public/             # Статические файлы
│   ├── package.json        # Node зависимости
│   ├── webpack.config.js   # Webpack конфигурация
│   ├── Dockerfile          # Multi-stage build
│   └── nginx.conf          # Nginx для продакшена
├── nginx/
│   └── nginx.conf          # Reverse proxy конфигурация
├── sql/init/               # Инициализация БД
│   ├── init_experiments_db.sql
│   └── init_users_db.sql
├── docker-compose.yml      # Оркестрация сервисов
├── .env                    # Переменные окружения
├── README.md               # Этот файл
├── DEPLOYMENT.md           # Руководство по развертыванию
└── ADMIN_PANEL.md          # Документация админ-панели
```

## 🔧 API Endpoints

### Аутентификация
- `POST /api/v1/auth/register` - Регистрация
- `POST /api/v1/auth/login` - Вход (JWT токены)
- `GET /api/v1/auth/me` - Текущий пользователь

### Пользователи
- `GET /api/v1/users/` - Список пользователей
- `POST /api/v1/users/` - Создать пользователя
- `GET /api/v1/users/{id}` - Получить пользователя
- `PUT /api/v1/users/{id}` - Обновить пользователя
- `DELETE /api/v1/users/{id}` - Удалить пользователя

### Эксперименты
- `GET /api/v1/experiments/` - Список экспериментов
- `POST /api/v1/experiments/` - Создать эксперимент
- `GET /api/v1/experiments/{id}` - Детали эксперимента
- `PUT /api/v1/experiments/{id}` - Обновить эксперимент
- `DELETE /api/v1/experiments/{id}` - Удалить эксперимент
- `GET /api/v1/experiments/user/{user_id}` - Эксперименты пользователя

### Типы Плёнок
- `GET /api/v1/films/` - Список типов плёнок
- `POST /api/v1/films/` - Создать тип плёнки
- `PUT /api/v1/films/{id}` - Обновить тип плёнки
- `DELETE /api/v1/films/{id}` - Удалить тип плёнки

### Конфигурации
- `GET /api/v1/equipment-configs/` - Список конфигураций
- `POST /api/v1/equipment-configs/` - Создать конфигурацию
- `PUT /api/v1/equipment-configs/{id}` - Обновить конфигурацию
- `DELETE /api/v1/equipment-configs/{id}` - Удалить конфигурацию

### Анализ
- `POST /api/v1/analysis/run/{experiment_id}` - Запустить анализ
- `GET /api/v1/analysis/results/{experiment_id}` - Получить результаты

Полная документация: http://localhost:8000/api/docs

## 🎨 Дизайн

- **Тема**: Темная с градиентными акцентами
- **Цвета**: Фиолетовый (#667eea) → Синий (#764ba2)
- **Типографика**: System fonts (SF Pro, Segoe UI)
- **Компоненты**: Модальные окна, карточки, таблицы, графики
- **Анимации**: Плавные переходы (250ms), fade-in эффекты
- **Адаптивность**: Mobile-first подход

## 🧪 Тестирование

```bash
# Backend тесты
pytest

# Frontend линтинг
cd frontend
npm run lint

# Проверка типов (если настроен TypeScript)
npm run type-check
```

## 📚 Документация

- [DEPLOYMENT.md](DEPLOYMENT.md) - Развертывание и настройка
- [ADMIN_PANEL.md](ADMIN_PANEL.md) - Админ-панель
- [API Docs](http://localhost:8000/api/docs) - Интерактивная документация Swagger

## 🔒 Безопасность

- ✅ JWT токены с коротким TTL
- ✅ Bcrypt хеширование паролей
- ✅ CORS защита
- ✅ Валидация входных данных (Pydantic)
- ✅ SQL injection защита (SQLAlchemy ORM)
- ✅ Раздельные базы данных для изоляции
- ⚠️ TODO: Rate limiting
- ⚠️ TODO: HTTPS в продакшене

## 🚧 Roadmap

### Высокий Приоритет
- [ ] Проверка роли администратора для админ-панели
- [ ] Пагинация для больших списков
- [ ] Экспорт результатов (Excel, PDF)
- [ ] Улучшенная обработка ошибок

### Средний Приоритет
- [ ] Поиск и фильтрация экспериментов
- [ ] Сравнение результатов нескольких экспериментов
- [ ] История изменений
- [ ] Email уведомления

### Низкий Приоритет
- [ ] Тёмная/светлая тема (переключение)
- [ ] Многоязычность (EN, RU)
- [ ] Импорт данных из CSV
- [ ] API для внешних систем

## 🐛 Known Issues

- Admin role checking not implemented (все авторизованные пользователи видят админ-панель)
- Pagination not implemented for large datasets
- Image upload size not validated on frontend

## 📞 Поддержка

Для вопросов и предложений создайте Issue в репозитории.

## 📄 Лицензия

Proprietary - все права защищены.

---

**Версия**: 1.0.0  
**Дата**: November 2025  
**Автор**: Created with Claude Sonnet 4.5
